# Ownership

Rust API owns authenticated shared facts. Bind identity from credentials, never caller role fields. New test migrations never target legacy databases.

Follow root implementation-first, consolidated-verification rules. Keep pure logic separate from I/O; add abstractions only for actual consumers.
